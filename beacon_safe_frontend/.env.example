# Beacon-Safe Frontend env (example)
# Vite only exposes variables prefixed with VITE_

# Backend API base URL (PreviewManager will expose backend on its own URL/port)
# Example:
# VITE_API_BASE=https://<backend-host>:3001
VITE_API_BASE=https://example-backend.kavia.ai:3001
